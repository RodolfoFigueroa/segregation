[project]
name = "segregation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "geopandas>=1.1.3",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "psycopg2>=2.9.11",
    "pyarrow>=23.0.1",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.48",
    "xarray>=2026.2.0",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D"]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
